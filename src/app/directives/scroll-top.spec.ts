import { ScrollTop } from './scroll-top';

describe('ScrollTop', () => {
  it('should create an instance', () => {
    const directive = new ScrollTop();
    expect(directive).toBeTruthy();
  });
});
